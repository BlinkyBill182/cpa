import type { ActionDefinition } from "@/lib/actions/types";

export const annualIncomeSummary: ActionDefinition = {
  key: "annual-income-summary",
  icon: "📊",
  dictNamespace: "annualIncomeSummary",
  scope: "office",
};
